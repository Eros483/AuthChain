# Frontend Overview
Handles UI as a React app. Planned functionality includes:
  - Pleasant UI (sike)
  - Ingest user request.
    - Objective is ordinary tool calling, and agentic work should be fine, so conversational form UI.
  - In case a sensitive tool is called, and blockchain service rejects it, then cautionary image of violation of terms of service.